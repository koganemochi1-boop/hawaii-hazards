{
  "name": "hawaii-hazards",
  "version": "2.1.6",
  "description": "Hawaiʻi natural hazards & preparedness — synthesis report for residents",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js",
    "validate": "node scripts/validate-content.js",
    "typecheck": "tsc",
    "refresh-data": "node scripts/refresh-bundled-data.js",
    "ci": "npm run validate && npm run typecheck && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
